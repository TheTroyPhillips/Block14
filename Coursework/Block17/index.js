const MAIN_INTEREST_RATE = .10;
//when const is not in a block and at the top of the screen it can be accesed by any block
if(true){
    let blockVar = "I am created in an if stmnt's block";
}
console.log(blockVar);

// this is a block when you see the braces
// when console log is on the outside of the block it will not
// run because it cannot be reached from inside the block

